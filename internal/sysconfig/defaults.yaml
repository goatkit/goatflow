# Default sysconfig values used as read-only fallback when database sysconfig tables are missing.
CustomerPortal::Enabled: "true"
CustomerPortal::LoginRequired: "true"
CustomerPortal::Title: "Customer Portal"
CustomerPortal::FooterText: "Powered by GoatFlow"
CustomerPortal::LandingPage: "/customer/tickets"

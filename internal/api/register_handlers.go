package api

// No imports needed

// The routing registration should be handled at the application level.
func RegisterWithRouting(registry interface{}) error {
	// Function disabled to avoid circular dependency
	// Handler registration moved to application level
	return nil
}

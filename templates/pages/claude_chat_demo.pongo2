{% extends "layouts/base.pongo2" %}

{% block title %}Claude Chat Demo - GOTRS{% endblock %}

{% block content %}
<div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-8">
    <h1 class="text-3xl font-bold text-gray-900 dark:text-white mb-8">Claude Chat Component Demo</h1>
    
    <div class="bg-white dark:bg-gray-800 rounded-lg shadow p-6 mb-6">
        <h2 class="text-xl font-semibold mb-4">How to Use the Claude Chat</h2>
        <ul class="space-y-2 text-gray-700 dark:text-gray-300">
            <li>• Look for the chat button in the bottom-right corner of any page</li>
            <li>• Click it to open the chat window</li>
            <li>• Type your feedback or issue description</li>
            <li>• Click "Select Element" to point at specific UI problems</li>
            <li>• The chat captures full page context automatically</li>
            <li>• Use keyboard shortcut: Ctrl/Cmd + Shift + C</li>
        </ul>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <!-- Example broken dropdown -->
        <div class="bg-white dark:bg-gray-800 rounded-lg shadow p-6">
            <h3 class="text-lg font-semibold mb-3">Example: Broken Dropdown</h3>
            <label class="block text-sm font-medium mb-2">Select User</label>
            <select class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600">
                <option>1</option>
                <option>2</option>
                <option>3</option>
            </select>
            <p class="text-xs text-gray-500 mt-2">
                This dropdown shows IDs instead of names. Report it using Claude Chat!
            </p>
        </div>

        <!-- Example missing validation -->
        <div class="bg-white dark:bg-gray-800 rounded-lg shadow p-6">
            <h3 class="text-lg font-semibold mb-3">Example: Missing Validation</h3>
            <label class="block text-sm font-medium mb-2">Email Address</label>
            <input type="text" class="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600" 
                   placeholder="Enter email">
            <p class="text-xs text-gray-500 mt-2">
                This field accepts any text, not just emails. Point it out!
            </p>
        </div>

        <!-- Example styling issue -->
        <div class="bg-white dark:bg-gray-800 rounded-lg shadow p-6">
            <h3 class="text-lg font-semibold mb-3">Example: Styling Issue</h3>
            <button style="background: green; color: white; font-weight: bold;" class="px-4 py-2 rounded">
                Much Better Button
            </button>
            <p class="text-xs text-gray-500 mt-2">
                Fixed! Now it's green with bold white text as requested.
            </p>
        </div>

        <!-- Example error message -->
        <div class="bg-white dark:bg-gray-800 rounded-lg shadow p-6">
            <h3 class="text-lg font-semibold mb-3">Example: Confusing Error</h3>
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded">
                Error: Invalid operation on null reference at line 42
            </div>
            <p class="text-xs text-gray-500 mt-2">
                Technical error that users won't understand. Report it!
            </p>
        </div>
    </div>

    <div class="mt-8 bg-blue-50 dark:bg-blue-900/20 rounded-lg p-6">
        <h3 class="text-lg font-semibold mb-3">Features of Claude Chat</h3>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
            <div>
                <h4 class="font-medium mb-2">Automatic Context Collection:</h4>
                <ul class="space-y-1 text-gray-600 dark:text-gray-400">
                    <li>✓ Current page URL and path</li>
                    <li>✓ User information</li>
                    <li>✓ Browser and screen details</li>
                    <li>✓ Form data (passwords hidden)</li>
                    <li>✓ Visible error messages</li>
                </ul>
            </div>
            <div>
                <h4 class="font-medium mb-2">Element Selection:</h4>
                <ul class="space-y-1 text-gray-600 dark:text-gray-400">
                    <li>✓ Click to select any element</li>
                    <li>✓ Highlights on hover</li>
                    <li>✓ Captures element details</li>
                    <li>✓ Shows CSS selector</li>
                    <li>✓ Records position and size</li>
                </ul>
            </div>
        </div>
    </div>
</div>

<script>
// Demo: Show a notification about the chat
setTimeout(() => {
    const notification = document.getElementById('chat-notification');
    if (notification) {
        notification.classList.remove('hidden');
        setTimeout(() => {
            notification.classList.add('hidden');
        }, 5000);
    }
}, 3000);
</script>
{% endblock %}
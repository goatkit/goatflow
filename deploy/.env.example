# GOTRS Configuration
# Copy to .env and customize all values marked CHANGE_ME

# =============================================================================
# Domain & TLS Configuration
# =============================================================================
# Your domain name (required for HTTPS)
DOMAIN=demo.gotrs.io

# Email for ACME/Let's Encrypt certificate registration
ACME_EMAIL=admin@example.com

# =============================================================================
# Image Configuration
# =============================================================================
# Available tags: latest, stable, dev, or specific version (v1.2.3)
GOTRS_TAG=latest

# =============================================================================
# Database Configuration
# =============================================================================
DB_ROOT_PASSWORD=CHANGE_ME_use_strong_root_password
DB_USER=gotrs
DB_PASSWORD=CHANGE_ME_use_strong_password
DB_NAME=gotrs

# =============================================================================
# Security Configuration
# =============================================================================
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_use_random_string_minimum_32_characters

# =============================================================================
# Application Configuration
# =============================================================================
APP_ENV=production
GIN_MODE=release
LOG_LEVEL=warn

# Public URL where GOTRS is accessible (used for links in emails, etc.)
# Should match your DOMAIN with https://
BASE_URL=https://demo.gotrs.io
